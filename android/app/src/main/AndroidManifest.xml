<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/AndroidManifest.xml -->
<!--
  FIX: Tambahkan 2 hal penting untuk koneksi ke Laravel lokal dari emulator:
  1. android:usesCleartextTraffic="true" — izinkan HTTP (bukan HTTPS)
     karena server lokal kita pakai http://, bukan https://
  2. <uses-permission INTERNET/> — izin akses internet
  
  Tanpa ini, Android 9+ memblokir semua HTTP request secara default.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- WAJIB: izin akses internet -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Untuk fitur image picker dari galeri -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:label="DonateID"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!--
          usesCleartextTraffic="true" mengizinkan HTTP (bukan HTTPS).
          Ini WAJIB untuk konek ke server Laragon lokal yang pakai HTTP.
          Saat production (server sudah pakai HTTPS), hapus baris ini.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
